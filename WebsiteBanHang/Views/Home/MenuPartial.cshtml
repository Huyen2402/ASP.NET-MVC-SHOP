@using WebsiteBanHang.Models;
@model IEnumerable<loaiSanPham>





<div class="grids-hny-2-mian py-5">
    <div style="margin: 8px auto; display: block; text-align:center;">

    </div>
    <h3 style="font-family:'Cambria Math'" class="hny-title mb-0 text-center">Mua theo thể loại</h3>

    <div style="margin: 8px auto; display: block; text-align:center;">

    </div>
    <div id="demos">
        <div class="owl-carousel owl-theme">
            @*@foreach (var item in Model)

        {
            <div class="item">
                <figure class="snip1577 boxhny13 ">
                    <img  src="@Url.Content("~/Content/images/"+item.HinhAnh)" alt="sample99" />
                    <figcaption>
                        <h3>@item.TenLoai</h3>
                        <h4>Luxery</h4>
                    </figcaption>
                    <a href="@Url.Action("ListSanPham","SanPham", new {@MaLSP = item.MaLoaiSP})"></a>
                </figure>
            </div>

        }*@
            @foreach (var item in Model)

            {<div class="items  snip1577 boxhny13 ">
                    <div class="row">

                        <h4 class="">@item.TenLoai</h4>
                        <img class="img-menupartial" src="@Url.Content(item.HinhAnh)" alt="Alternate Text" />

                        <a href="@Url.Action("ListSanPham","SanPham", new {@MaLSP = item.MaLoaiSP})"></a>
                    </div>
                </div>
            }
        </div>
            </div>

        </div>
        <style>

            #demos .owl-carousel .items {
              min-height: 172px;
             
                padding: 1rem;
            }
            .img-menupartial {
                min-height: 172px;
            }
           
        </style>

        <script>
            $('.owl-carousel').owlCarousel({
                items: 6,
                loop: true,
                margin: 10,
                autoplay: true,
                autoplayTimeout: 3000,
                autoplayHoverPause: true
            })
        </script>
